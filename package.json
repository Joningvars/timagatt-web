{
  "name": "timagatt",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:app": "turbo run dev --filter=@timagatt/app",
    "dev:marketing": "turbo run dev --filter=@timagatt/marketing",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}

